<Window x:Class="Vistas.ValidarAlumno"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:control="clr-namespace:Vistas.ControlUsuario"
        Title="ValidarAlumno" Height="533" Width="623" Background="#f8f0ec">
    
   
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ENCABEZADO CON TÍTULO Y LOGO -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- TÍTULO PRINCIPAL CENTRADO -->
            <Label Content="VALIDAR ALUMNO" 
                   Style="{StaticResource LabelTitulo}" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Grid.Column="0" Grid.ColumnSpan="2"/>

            <!-- LOGO EN ESQUINA SUPERIOR DERECHA -->
            <Image Source="/Images/logo-lujan.png" Grid.Column="1"
                   Width="60" Height="60" 
                   HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>

        <!-- CONTROL DE USUARIO -->
        <control:AlumnoControl x:Name="altaAlumno" Grid.Row="1"
                           Margin="0,5,0,0"/>

        <!-- BOTONES CENTRADOS -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,15,0,0">
            <Button Name="btnValidar" Content="Validar" Width="120" Height="40"
                    Style="{StaticResource btnAceptar}" 
                    Click="btnValidar_Click" Margin="0,0,15,0"/>
            <Button Name="btnCancelar" Content="Cancelar" Width="120" Height="40"
                    Style="{StaticResource btnCancelar}" 
                    Click="btnCancelar_Click"/>
        </StackPanel>

    </Grid>

</Window>
